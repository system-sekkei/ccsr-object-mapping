<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="example.datasource._sample.ActionMapper">
    <select id="list" resultType="example.domain.model._sample.Action">
        SELECT
            アクション.管理番号 as "identity",
            アクション.名称 as "name"
        FROM サンプル.アクション
        ORDER BY created_at DESC
    </select>

    <insert id="register">
        INSERT INTO サンプル.アクション
        (管理番号,名称)
        VALUES
        (#{identity}, #{name})
    </insert>
</mapper>